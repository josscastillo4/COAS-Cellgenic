# types/

Shared TypeScript types and interfaces (e.g. `coa.ts`, `product.ts`, `user.ts`).
`navigation.ts` is already defined here for the Sidebar. Add one file per
domain entity as business logic is introduced, and avoid defining shared
types inline inside components.
