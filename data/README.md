# data/

Static/reference data, mock fixtures, and seed data used during development
before a real database is connected (e.g. sample COA/product JSON for local
testing). Not intended for production data storage.
