# hooks/

Custom React hooks (e.g. `useCoas`, `useAuth`, `useDebounce`) will live here
once business logic is introduced. Keep hooks framework-agnostic where
possible and free of direct fetch/database calls — delegate those to `services/`.
