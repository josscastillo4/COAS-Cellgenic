# services/

Data-access and integration layer (e.g. `coaService.ts`, `authService.ts`,
`wordpressService.ts`, `storageService.ts`). This is where future database
queries, API calls, and WordPress integration logic will be implemented,
kept separate from UI components and hooks.
